{
  "name": "spark",
  "version": "2.3.0",
  "private": true,
  "description": "Spark — Offline-first gratitude, wins & affirmations app with bend-not-break streaks.",
  "main": "src/app.tsx",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "test": "jest --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "expo": "latest",
    "expo-sqlite": "latest",
    "react": "18.x",
    "react-native": "0.75.x"
  },
  "devDependencies": {
    "@testing-library/react-native": "^12.4.5",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "react-test-renderer": "18.x",
    "ts-jest": "^29.1.2",
    "typescript": "^5.5.4"
  },
  "jest": {
    "preset": "react-native",
    "transformIgnorePatterns": [
      "node_modules/(?!(react-native|@react-native|expo|expo-sqlite)/)"
    ],
    "testMatch": [
      "**/__tests__/**/*.test.[jt]s?(x)"
    ]
  }
}
